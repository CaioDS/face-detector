const USERS_IMAGE_DIRECTORY = './api/utils/assets/users';

const RECOGNIZE_IMAGE_DIRECTORY = './api/utils/assets/recognize';

module.exports = {
  USERS_IMAGE_DIRECTORY,
  RECOGNIZE_IMAGE_DIRECTORY,
};
